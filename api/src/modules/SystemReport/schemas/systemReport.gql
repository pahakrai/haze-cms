scalar Date

type SystemReport {
  _id: ID!

  # generated by mongoose {timestamp: true}
  createdAt: Date
  updatedAt: Date
}

input SystemReportCreateModel {
  name: String!
}

input SystemReportUpdateModel {
  name: String
}

type PaginateSystemReport {
  nodes: [SystemReport]
  startCursor: ID
  endCursor: ID
  nodeCount: Float
  total: Float
  isEnd: Boolean
}

input SystemReportSearchModel {
  q: String
}

type Query {
  systemReport(id: ID!): SystemReport
  systemReports(
    query: SystemReportSearchModel,
    paginate: Paginate,
    options: QueryOption
  ): PaginateSystemReport
}

type Mutation {
  createSystemReport(systemReportCreateModel:SystemReportCreateModel): SystemReport
  updateSystemReport(id: ID!, systemReportUpdateModel:SystemReportUpdateModel): SystemReport
}
