/* @flow */

import React from 'react';

import DocumentTitle from '../Components/Common/DocumentTitle';
import Logout from '../Containers/Logout';

export default () => (
  <DocumentTitle title={'Logout Page'}>
    <Logout />
  </DocumentTitle>
);
