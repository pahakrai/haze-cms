import React from 'react';

import Viewport from '../Components/App/Meta/Viewport';
class Metas extends React.PureComponent {
  render() {
    return (
      <React.Fragment>
        <Viewport />
      </React.Fragment>
    );
  }
}
export default Metas;
