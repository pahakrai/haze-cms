const { paginate } = require('../../constant');

const e = (module.exports = {});

e.point = `
});

/* ------------- Reducers ------------- */`;
e.content = `,
  ${paginate}: DEFAULT_PAGINATION_SETTING`;
